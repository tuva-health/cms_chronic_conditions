version: 2

sources:
  - name: core
    database: "{{ var('chronic_conditions_input_database','tuva') }}"
    schema:  "{{ var('chronic_conditions_input_schema','core') }}"
    tables:
      - name: condition
      - name: encounter
      - name: prescription
      - name: procedure
