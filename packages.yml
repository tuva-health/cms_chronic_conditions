packages:
  # Used by cms_chronic_conditions models: pivot, get_column_values.
  - package: dbt-labs/dbt_utils
    version: [">=1.2.0", "<2.0.0"]

  # the_tuva_project (tuva-core) provides load_package_seed (used by the seed
  # post-hook in dbt_project.yml) and the core__* models that the default
  # pharmacy_claim / encounter / condition / procedure / patient vars ref.
  # Pinned to the reviewed tuva-core architecture commit so the 1.0 fleet
  # resolves the shared nested-path asset loader.
  # TODO(D2): switch to hub range [">=1.0.0", "<1.1.0"]
  - git: "https://github.com/tuva-health/tuva-core.git"
    revision: c14dc2c86545f5c9c49347c43ba683ebed299311
